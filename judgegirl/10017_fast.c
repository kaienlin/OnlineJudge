#include <stdio.h>
#include <stdlib.h>
#define MAXN 50000
#define MAXSIZE 32768

short board[MAXSIZE][MAXSIZE] = {{0}};

int main()
{
    int N;
    scanf("%d", &N);
    int x, y;
    int Acnt = 0;
    while (N--) {
        scanf("%d%d", &x, &y);
    }
    return 0;
}