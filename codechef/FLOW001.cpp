#include <bits/stdc++.h>
#define IOS { ios::sync_with_stdio(false); cin.tie(nullptr); }

using namespace std;

int main()
{
    IOS;
    int T; cin >> T;
    while ( T-- ) {
        int A, B; cin >> A >> B;
        cout << A + B << '\n';
    }
    return 0;
}
